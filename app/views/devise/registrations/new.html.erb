<% provide(:title, "Sign up") %>
<div class="book_for_in">
  <div class="side_page_in" >
    <%= devise_error_messages! %>
    <p>Впишите свое имя в гостевую книгу!<br><br>
      Присоеденяйтесь к сообществу и насладитесь уютной <br>
      атмосферой.<br>
      <%= link_to image_tag('logo-sm.png', alt: "blog-logo"), root_path %> <br><br>
      Если вы уже явлетесь членом сообщества <br>
      то ищите свое имя в гостевой книге<br><br>
    <%= link_to "Войти", new_user_session_path, class:'btn btn-warning' %>
    </p>

  </div>
  <h2>Регистрация</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

  <div class="field form-group">
    <%= f.label :name, "Имя" %><br />
    <%= f.text_field :name,class: "form-control form-width", autofocus: true %>
  </div>

  <div class="field form-group">
    <%= f.label :email %><br />
    <%= f.email_field :email,class: "form-control form-width", autofocus: true %>
  </div>

  <div class="field form-group">
    <%= f.label :password, "Пароль" %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> символов минимум)</em>
    <% end %><br />
    <%= f.password_field :password,class: "form-control form-width", autocomplete: "off" %>
  </div>

  <div class="field form-group">
    <%= f.label :password_confirmation, "Подтверждение пароля" %><br />
    <%= f.password_field :password_confirmation,class: "form-control form-width", autocomplete: "off" %>
  </div>

  <div class="actions button-margin">
    <%= f.submit "Регистрация", class: "btn btn-success" %>
  </div>
<% end %>
</div>
