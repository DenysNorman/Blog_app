<% provide(:title, "Log in") %>
<div class="book_for_in">
  <div class="side_page_in" >
    <p class="messages">
      <span class="notice"><%= notice %></span>
      <span class="alert"><%= alert %></span></p>
    <p>Ищите свое имя в гостевой книге!<br><br>
      Присоеденяйтесь к нащей компании и насладитесь <br>
      атмосферой.<br>
      <%= link_to image_tag('logo_exsm.png', alt: "blog-logo"),root_path %> <br><br><br><br><br>
      Если вы не записаны в гостевой книге<br>
      попросите дворецкого вписать ваше имя<br><br>
      <%= link_to "Регистрация",  new_user_registration_path, class:'btn btn-warning' %>
    </p>

  </div>
  <h2>Вход</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field form-group">
      <%= f.label :email %><br />
      <%= f.email_field :email,class: "form-control form-width", autofocus: true %>
    </div>

    <div class="field form-group">
      <%= f.label :password, 'Пароль' %><br />
      <%= f.password_field :password,class: "form-control form-width", autocomplete: "off" %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, 'Запомниться дворецкому' %>
      </div>
    <% end -%>

    <div class="actions">
      <%= f.submit "Вход", class: "btn btn-success" %>
    </div>
  <% end %>
</div>