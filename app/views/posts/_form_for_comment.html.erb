<%= form_for [@post, @post.comments.new(parent_id: params[:parent_id])], remote: true do|f| %>
    <fieldset>
      <ul>
        <%= f.hidden_field :parent_id %>
        <li>
          <%= f.text_area :body, class:'form-control', rows: 2, cols: 60,
                          placeholder: 'Дайте комментарий...' %>
        </li>
        <li class="button_margin_comment">
          <%= f.submit 'Прокомментировать', class: "btn btn-success btn-sm" %>
        </li>
      </ul>
    </fieldset>
<% end %>